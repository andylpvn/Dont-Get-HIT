/// @description is_between(val, small, big)
/// @param val
/// @param  small
/// @param  big
function is_between(argument0, argument1, argument2) {

	var val = argument0;
	var small = argument1;
	var big = argument2;

	return val >= small && val <= big;



}
